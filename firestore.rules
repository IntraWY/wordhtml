rules_version = '2';

// Recommended rules for wordhtml after Firebase Auth + per-user paths are implemented.
// CURRENT APP: may still use legacy global `templates` — adjust rules to match your deployment.
//
// Deploy: firebase deploy --only firestore:rules
//
// See docs/firebase-cloud-sync-design.md

service cloud.firestore {
  match /databases/{database}/documents {

    // --- Target (per-user) layout ---
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      match /templates/{templateId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /snapshots/{snapshotId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }

    // --- Legacy global templates (REMOVE or deny after migration) ---
    // WARNING: open rules allow anyone with your API key to read/write.
    // Use only for development, or replace with Auth-scoped rules.
    match /templates/{templateId} {
      // Development-only example — tighten before production:
      allow read, write: if true;
    }
  }
}
